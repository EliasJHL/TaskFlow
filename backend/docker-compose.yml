version: '3.8'

services:
  postgres-db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "51214:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
